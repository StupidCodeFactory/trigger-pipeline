name: trigger-pipeline
version: 0.0.1
description: Trigger a pipeline, usefule if you want to build cross application worflows
keywords:
  - pipeline-id
  - api-token
  - max-attempts
  - delay
properties:
  pipeline-id:
    type: string
    required: true
  api-token:
    type: string
    required: true
  max-attempts:
    type: integer
    required: false
  delay:
    type: string
    required: false
